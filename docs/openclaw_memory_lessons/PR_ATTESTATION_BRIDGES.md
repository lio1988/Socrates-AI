# Draft PR Summary — Governed Attestation Bridges

Adds two explicit named-human bridges and one curator report:

1. matched Identity resolution evidence from adjacent shadow failure/win windows;
2. Soul constitutional/risk attestation over existing same-agent evidence;
3. `SELF_REVISION_PIPELINE.md` from `openclaw_review.py`.

All paths are offline, append-only, non-self-attested, and evidence-only. Nothing
is automatically proposed, approved, applied, promoted, or granted authority.

Local verification commands are documented in
`ATTESTATION_BRIDGES_TEST_GATE.md`.
