# fix/socratic-acceptance-contract

This isolated baseline branch closes the deterministic acceptance gap between a
provider-valid `AgentMove` envelope and a constitutionally valid Socratic move.
It also contains the subsequent, separately committed hardening of the existing
task-kind epistemic-marker contract.

It starts at baseline `277ca2ec130ce120dba9c4d894a3c58138b25528` and does not
contain SocratesZero search changes.
