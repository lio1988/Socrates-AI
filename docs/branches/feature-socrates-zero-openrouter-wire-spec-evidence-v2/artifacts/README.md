# Phase 8.5D-S recovery artifacts

`openrouter_wire_specification_manifest_validation_v2.json` records the bounded
result of the additive evidence recovery.

The artifact supports only the next wire-mapping authorization decision. It
does not authorize parser implementation or a live call. It explicitly keeps
exact endpoint response identity, P17, P18, P19, credential/network controls,
and live-pilot readiness unresolved.

The inherited provenance-inventory test remains separately deselected in the
recorded OpenRouter regression surface; v2 does not weaken or modify that
boundary.
