# OpenRouter official wire-specification evidence v2

The authoritative file in this folder is:

`openrouter_official_wire_specification_manifest_v2.json`

It is pinned to the official `OpenRouterTeam/docs` repository at commit:

`4a5a458dbb6a0041db0480c17ab67c4c1a3ae0db`

Each source record includes an exact official path, Git blob SHA and byte size.
The manifest contains only directly documented facts or lossless mappings.

It deliberately does not claim exact response-side endpoint identity, live
conformance, input-token closure, pricing, total-cost closure, or live-pilot
readiness.
