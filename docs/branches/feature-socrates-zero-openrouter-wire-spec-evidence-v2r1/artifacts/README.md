# Artifacts — OpenRouter Wire Specification v2r1

Write-once evidence:

- `openrouter_wire_specification_manifest_validation_v2r1.json`
- `openrouter_wire_specification_manifest_revalidation_v2r1.json`

Validation status: `SUPPORTED`.

Offline revalidation confirms semantic equality, manifest-ID equality, validation-result equality, manifest byte identity and validation byte identity. Post-result tests do not rewrite these files.
