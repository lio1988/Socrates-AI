# Branch: feature/socrates-zero-successor-failure-semantics-v1

## Purpose

Perform the read-only Phase 8R architecture and experimental-design gate after
the sealed Phase 8 v1 taxonomy falsification. Map identity dependencies and
actual validation order, select one failure-precedence model, distinguish
orthogonal from precedence probes, and pre-register the smallest defensible new
experiment without changing runtime semantics.

## Success criterion

The branch is complete when it preserves and verifies the sealed falsified
artifact, explains the `wrong-provider` result from repository source, freezes
one unambiguous precedence rule and full new probe matrices, chooses exactly one
Phase 8R decision, recommends exactly one next engineering branch, and passes
the allowed documentation/integrity/regression gates with zero aggregate or
provider calls.

## Scope

- documentation and pre-registration only;
- canonical identity dependency and council-roster/provider analysis;
- static validation-order and failure-layer inventory;
- provider/model/config reachability classification;
- failure-precedence, orthogonal-probe, and precedence-probe design;
- version and freeze chronology for a possible new experiment.

The canonical decision report is
[`../../SOCRATES_ZERO_PHASE8R_FAILURE_PRECEDENCE_DECISION_GATE.md`](../../SOCRATES_ZERO_PHASE8R_FAILURE_PRECEDENCE_DECISION_GATE.md).

## Non-goals

No v1 aggregate rerun, artifact rewrite, provider call, observation recording,
runtime or error-code change, context weakening, Phase 8.5, shadow execution,
depth two, Experience Store, learning, RL, or production authority.

Branch context: [MEMORY.md](MEMORY.md) · [PLAN.md](PLAN.md) ·
[PRESENT.md](PRESENT.md)
