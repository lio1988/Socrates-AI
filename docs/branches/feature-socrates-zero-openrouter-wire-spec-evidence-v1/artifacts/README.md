# Phase 8.5D-S artifacts

- `openrouter_wire_specification_manifest_validation_v1.json`: authoritative
  sufficiency result (`FALSIFIED`).
- `openrouter_wire_specification_manifest_revalidation_v1.json`: independent
  offline recomputation proving semantic, ID, validation-result and byte
  equality without a second documentation retrieval.

The artifacts establish deterministic preservation of the negative result. They
do not establish that the official OpenRouter documentation itself is
insufficient.
