# Memory

The preceding manifest v1 was falsified because the assistant completion container produced six network errors before receiving official bytes. This recovery does not relabel that result. It creates a separate execution lineage on the user's Windows environment, where public HTTPS access is available.

The anti-loop limit remains strict: one six-source execution, zero retries, no source substitution, and no immediate second recovery version.
