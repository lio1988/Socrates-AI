# Recovery evidence

The write-once recovery receipt will be created here after the local six-source execution. The new retrieval log is stored alongside the predecessor evidence as `openrouter_wire_retrieval_log_v1r1.json`; retained snapshots use the frozen v1 evidence paths and are new files only on this recovery branch.
